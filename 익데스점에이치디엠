<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>EcoBridge Foundation — 청소년 생태 재단</title>
<meta name="description" content="EcoBridge 재단 — 학교와 지역사회를 잇는 청소년 중심의 생태·환경 활동과 캠페인" />
<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><circle cx='50' cy='50' r='48' fill='%2300A86B'/><path d='M30 60 Q50 20 70 60' stroke='%23fff' stroke-width='6' fill='none' stroke-linecap='round'/></svg>">
<style>
  /* --------- 기본 리셋 & 타이포그래피 --------- */
  :root{
    --bg:#0f1724;
    --card:#0b1220;
    --muted:#98A0B3;
    --accent:#00A86B; /* 초록 */
    --accent2:#00BFFF; /* 하늘 */
    --glass: rgba(255,255,255,0.03);
    --radius:12px;
    --maxw:1000px;
  }
  *{box-sizing:border-box}
  html,body{height:100%}
  body{
    margin:0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Noto Sans KR", "Apple SD Gothic Neo", "Malgun Gothic", Arial;
    background:
      radial-gradient(1200px 600px at 10% 10%, rgba(0,184,255,0.03), transparent),
      linear-gradient(180deg, #071022 0%, #071421 60%);
    color: #E6EEF6;
    -webkit-font-smoothing:antialiased;
    line-height:1.6;
    padding:32px 16px;
    display:flex;
    justify-content:center;
  }
  .wrap{width:100%; max-width:var(--maxw);}

  /* --------- 헤더 / 네비 --------- */
  header{
    display:flex;
    align-items:center;
    gap:18px;
    margin-bottom:28px;
  }
  .logo{
    display:flex;
    gap:12px;
    align-items:center;
    text-decoration:none;
    color:inherit;
  }
  .logo-svg{
    width:56px;
    height:56px;
    flex:0 0 56px;
    display:inline-block;
  }
  .brand{
    line-height:1;
  }
  .brand h1{font-size:18px;margin:0}
  .brand p{margin:0;font-size:12px;color:var(--muted)}
  nav{margin-left:auto; display:flex; gap:12px; align-items:center;}
  .btn{
    background:linear-gradient(90deg,var(--accent),var(--accent2));
    border:none;color:#051018;padding:10px 14px;border-radius:10px;font-weight:600;cursor:pointer;
    box-shadow:0 6px 18px rgba(0,160,110,0.12);
  }
  .ghost{
    background:transparent;border:1px solid rgba(255,255,255,0.06);padding:8px 12px;border-radius:10px;color:var(--muted);
  }

  /* --------- 히어로 섹션 --------- */
  .hero{
    display:grid;
    grid-template-columns: 1fr 340px;
    gap:20px;
    align-items:start;
    margin-bottom:28px;
  }
  .card{
    background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
    border-radius:var(--radius);
    padding:20px;
    box-shadow: 0 8px 30px rgba(2,6,23,0.6);
    border:1px solid rgba(255,255,255,0.03);
  }
  .hero h2{margin:0 0 8px 0;font-size:24px}
  .hero p.lead{color:var(--muted); margin:0 0 18px 0}
  .slogan-list{display:flex;flex-direction:column;gap:8px}
  .slogan{background:var(--glass); padding:10px;border-radius:10px;color:#dff5ea;font-weight:600;font-size:14px}

  /* --------- 사이드(구독 + 핵심 활동) --------- */
  .side {
    display:flex;flex-direction:column;gap:12px;
  }
  .subscribe input[type="email"]{
    width:100%; padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.06);
    background:transparent;color:inherit;margin-bottom:8px;
  }
  .small{font-size:13px;color:var(--muted)}

  /* --------- 블로그 포스트 리스트 --------- */
  .posts{display:grid;grid-template-columns:repeat(2,1fr);gap:16px;margin-bottom:28px;}
  .post{display:flex;gap:12px;align-items:flex-start}
  .thumb{
    width:110px;height:80px;border-radius:10px;flex:0 0 110px;background:linear-gradient(135deg,var(--accent),var(--accent2));
    display:flex;align-items:center;justify-content:center;font-weight:700;color:#051018;
  }
  .post h3{margin:0 0 6px 0;font-size:16px}
  .meta{color:var(--muted);font-size:13px}

  /* --------- 핵심 활동 상세 카드 --------- */
  .activity{
    display:flex;gap:14px;align-items:center;margin-bottom:18px;
  }
  .activity .icon{width:64px;height:64px;border-radius:12px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));display:flex;align-items:center;justify-content:center;font-size:22px}
  .cta-row{display:flex;gap:10px;margin-top:12px}

  /* --------- footer --------- */
  footer{margin-top:20px;text-align:center;color:var(--muted);font-size:13px;padding:18px 0}

  /* --------- 반응형 --------- */
  @media (max-width:880px){
    .hero{grid-template-columns:1fr; }
    .posts{grid-template-columns:1fr;}
    nav{display:none}
  }

  /* 작고 세련된 애니메이션 */
  .fade-up{transform:translateY(6px);opacity:0;animation:fadeUp .6s ease forwards}
  @keyframes fadeUp{to{transform:none;opacity:1}}
</style>
</head>
<body>
  <div class="wrap">
    <!-- 헤더 -->
    <header>
      <a class="logo" href="#">
        <!-- 간단한 SVG 로고: 나뭇잎 + 다리 모티프 -->
        <svg class="logo-svg" viewBox="0 0 120 120" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="EcoBridge 로고">
          <defs>
            <linearGradient id="g1" x1="0" x2="1" y1="0" y2="1">
              <stop offset="0" stop-color="#00BFFF"/>
              <stop offset="1" stop-color="#00A86B"/>
            </linearGradient>
          </defs>
          <rect width="120" height="120" rx="20" fill="url(#g1)"/>
          <path d="M18 78 C38 36, 82 36, 102 78" stroke="#051018" stroke-width="6" fill="none" stroke-linecap="round"/>
          <path d="M56 38 C50 26, 36 24, 28 40 C34 38,44 46,56 38 Z" fill="#051018" opacity="0.12"/>
        </svg>
        <div class="brand">
          <h1>EcoBridge 재단</h1>
          <p>Connect Nature, Create Future</p>
        </div>
      </a>

      <nav>
        <button class="ghost" onclick="document.getElementById('posts').scrollIntoView({behavior:'smooth'})">블로그</button>
        <button class="ghost" onclick="document.getElementById('activity').scrollIntoView({behavior:'smooth'})">핵심활동</button>
        <button class="btn" id="joinBtn">참여하기</button>
      </nav>
    </header>

    <!-- 히어로 -->
    <section class="hero">
      <div class="card fade-up" style="animation-delay:.05s">
        <h2>지구와 나를 잇다 — 작은 행동이 큰 변화를 만듭니다</h2>
        <p class="lead">EcoBridge는 청소년이 주도하는 비영리 재단으로, 학교와 지역사회에서 실천 가능한 생태 복원과 탄소 저감 캠페인을 운영합니다. 교육, 캠페인, 현장 복원 활동을 통해 모두가 친환경 습관을 만들 수 있도록 지원합니다.</p>

        <div style="display:flex;gap:12px;flex-wrap:wrap">
          <div style="min-width:220px">
            <strong>핵심 활동</strong>
            <p class="small" id="activity-summary">학교와 지역사회 '생활 속 탄소 줄이기 캠페인' — 플로깅, 분리배출 챌린지, 지역 생태정원 가꾸기</p>
          </div>
          <div>
            <strong>로고 컨셉</strong>
            <p class="small">나뭇잎 + 다리 + 지구 — 세대를 잇는 다리 역할</p>
          </div>
        </div>

        <div style="margin-top:14px" class="slogan-list" aria-hidden>
          <div class="slogan">"지구와 나를 잇다, 작은 행동에서 미래가 자란다"</div>
          <div class="slogan">"청소년이 만드는 푸른 다리, 지속가능한 내일로"</div>
          <div class="slogan">"하나뿐인 지구, 함께 지키는 첫걸음"</div>
        </div>
      </div>

      <!-- 사이드: 구독 / 빠른 참여 -->
      <aside class="side">
        <div class="card subscribe fade-up" style="animation-delay:.12s">
          <h3 style="margin:0 0 8px 0">뉴스레터 구독</h3>
          <p class="small" style="margin:0 0 12px 0">재단 소식, 캠페인 일정, 작은 실천 팁을 받아보세요.</p>
          <input type="email" id="email" placeholder="이메일 주소 입력" aria-label="이메일 입력">
          <button class="btn" id="subscribeBtn">구독하기</button>
          <p id="subMsg" class="small" style="margin-top:8px"></p>
        </div>

        <div class="card fade-up" style="animation-delay:.16s">
          <h4 style="margin:0 0 8px 0">빠른 참여</h4>
          <div class="activity">
            <div class="icon">🌱</div>
            <div>
              <strong>플로깅 & 정원 가꾸기</strong>
              <p class="small" style="margin:6px 0 0 0">주말 커뮤니티 클린업에 참여해 보세요. 장비와 안내는 재단이 지원합니다.</p>
              <div class="cta-row">
                <button class="btn" onclick="alert('참여 신청 폼으로 이동(예시).')">신청하기</button>
                <button class="ghost" onclick="document.getElementById('posts').scrollIntoView({behavior:'smooth'})">자세히</button>
              </div>
            </div>
          </div>
        </div>

      </aside>
    </section>

    <!-- 블로그 포스트들 -->
    <main>
      <section id="posts" class="card" style="margin-bottom:18px">
        <h2 style="margin-top:0">최근 활동 & 블로그</h2>
        <p class="small" style="margin-top:4px;margin-bottom:12px">현장 리포트, 캠페인 안내, 실천 팁 등 — 학생들이 직접 쓰는 이야기</p>

        <div class="posts">
          <article class="post fade-up" style="animation-delay:.18s">
            <div class="thumb">PLG</div>
            <div>
              <h3>학교 플로깅 캠페인 후기 — 3km에 담긴 이야기</h3>
              <div class="meta">작성자: 학생운영팀 · 2025-10-12</div>
              <p class="small" style="margin-top:8px">플로깅으로 모인 쓰레기 종류와 재활용 팁, 다음 캠페인 일정 안내.</p>
            </div>
          </article>

          <article class="post fade-up" style="animation-delay:.20s">
            <div class="thumb">GRD</div>
            <div>
              <h3>교내 생태정원 만들기 — 씨앗부터 꽃피기까지</h3>
              <div class="meta">작성자: 봉사팀 · 2025-09-05</div>
              <p class="small" style="margin-top:8px">학생 주도의 정원 설계 과정과 초보자 식물 관리 가이드.</p>
            </div>
          </article>

          <article class="post fade-up" style="animation-delay:.22s">
            <div class="thumb">TIP</div>
            <div>
              <h3>분리배출 챌린지: 7일 실천 체크리스트</h3>
              <div class="meta">작성자: 캠페인팀 · 2025-08-21</div>
              <p class="small" style="margin-top:8px">매일 하기 쉬운 분리배출 팁과 교내 리워드 아이디어.</p>
            </div>
          </article>

          <article class="post fade-up" style="animation-delay:.24s">
            <div class="thumb">EDU</div>
            <div>
              <h3>학교 수업과 연결하는 생태교육 가이드</h3>
              <div class="meta">작성자: 교육팀 · 2025-07-30</div>
              <p class="small" style="margin-top:8px">통합과학 커리큘럼과 연계한 활동 예시와 평가 방법.</p>
            </div>
          </article>
        </div>
      </section>

      <!-- 핵심 활동 상세 -->
      <section id="activity" class="card" style="margin-bottom:12px">
        <h2 style="margin-top:0">핵심 활동 — 생활 속 탄소 줄이기 캠페인</h2>
        <p class="small">EcoBridge의 주요 프로젝트는 ‘학교와 지역사회에서 실천 가능한 탄소 저감 활동’을 확산하는 것입니다. 아래 구성은 언제든 실제 프로젝트 계획으로 확장 가능합니다.</p>

        <div style="margin-top:14px">
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
            <div>
              <h4>1) 플로깅 & 커뮤니티 클린업</h4>
              <p class="small">정기 플로깅을 통해 쓰레기 수거 · 분류 교육을 병행합니다. 수거 데이터는 캠페인 보고서로 공유합니다.</p>
            </div>
            <div>
              <h4>2) 분리배출·제로웨이스트 챌린지</h4>
              <p class="small">한 학기 단위 챌린지로 참여자 인증 시스템을 운영하고, 우수 활동팀에는 소정의 시상·인증서를 제공합니다.</p>
            </div>
            <div>
              <h4>3) 학교 생태정원 만들기</h4>
              <p class="small">지역 식물을 사용한 작은 정원을 조성해 도시 생물다양성 회복을 돕습니다. 학생 주도 설계와 유지관리 교육 포함.</p>
            </div>
            <div>
              <h4>4) 교육·워크숍</h4>
              <p class="small">기초 생태 지식, 쓰레기 분류, 지속가능한 소비에 관한 워크숍을 운영합니다.</p>
            </div>
          </div>
        </div>
      </section>

      <!-- 간단한 연락/문의 폼(예시) -->
      <section class="card" style="margin-bottom:8px">
        <h3 style="margin-top:0">문의하기</h3>
        <p class="small">프로젝트 제안, 협력 요청, 자원봉사 문의는 아래 폼을 이용해주세요.</p>
        <form id="contactForm" onsubmit="return false" style="display:grid;gap:8px;margin-top:10px">
          <input type="text" id="name" placeholder="이름" required style="padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.06);background:transparent;color:inherit">
          <input type="email" id="cemail" placeholder="이메일" required style="padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.06);background:transparent;color:inherit">
          <textarea id="message" placeholder="문의 내용" rows="4" style="padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.06);background:transparent;color:inherit"></textarea>
          <div style="display:flex;gap:8px">
            <button class="btn" onclick="submitContact()">전송</button>
            <button class="ghost" type="reset">초기화</button>
          </div>
          <div id="contactMsg" class="small"></div>
        </form>
      </section>

    </main>

    <footer>
      © <strong>EcoBridge 재단</strong> · 청소년 주도 생태활동 · 모든 이미지는 예시입니다.
    </footer>
  </div>

<script>
  // --- 구독 버튼 처리 (간단한 프론트엔드 검증) ---
  document.getElementById('subscribeBtn').addEventListener('click', function(){
    const email = document.getElementById('email').value.trim();
    const msg = document.getElementById('subMsg');
    if(!email) {
      msg.textContent = '이메일을 입력해 주세요.';
      return;
    }
    // 간단한 이메일 패턴 체크
    const re = /^[^@\s]+@[^@\s]+\.[^@\s]+$/;
    if(!re.test(email)){
      msg.textContent = '올바른 이메일 형식이 아닙니다.';
      return;
    }
    // (실제 전송은 백엔드 필요) 여기서는 간단히 메시지 표시
    msg.style.color = '#BEECC6';
    msg.textContent = '구독해주셔서 감사합니다! 곧 소식지를 보내드릴게요(예시).';
    document.getElementById('email').value = '';
  });

  // --- 연락 폼 전송 시 간단한 처리(프론트엔드 시뮬레이션) ---
  function submitContact(){
    const name = document.getElementById('name').value.trim();
    const email = document.getElementById('cemail').value.trim();
    const message = document.getElementById('message').value.trim();
    const out = document.getElementById('contactMsg');

    if(!name || !email || !message){
      out.style.color = '#FFB4B4';
      out.textContent = '모든 항목을 채워주세요.';
      return;
    }
    // 간단한 이메일 체크
    const re = /^[^@\s]+@[^@\s]+\.[^@\s]+$/;
    if(!re.test(email)){
      out.style.color = '#FFB4B4';
      out.textContent = '이메일 형식이 올바르지 않습니다.';
      return;
    }

    // 실제 전송은 서버가 필요 -> 여기선 시뮬레이션
    out.style.color = '#BEECC6';
    out.textContent = '문의가 접수되었습니다. 감사합니다! (예시 동작)';
    // 입력 초기화
    document.getElementById('contactForm').reset();
  }

  // --- 참여하기 버튼 클릭 예시 (스크롤/포커스) ---
  document.getElementById('joinBtn').addEventListener('click', function(){
    document.getElementById('contactForm').scrollIntoView({behavior:'smooth'});
    document.getElementById('name').focus();
  });

  // --- 간단한 접근성: 이미지/텍스트 로드 애니메이션 적용 ---
  window.addEventListener('load', function(){
    document.querySelectorAll('.fade-up').forEach((el,i)=>{
      el.style.animationDelay = (0.05 + i*0.04) + 's';
    });
  });
</script>
</body>
</html>
